global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'cqrs-order-service'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['app:9090']             # Docker full-stack (make docker-up)
        labels:
          env: 'docker'
      - targets: ['172.22.0.1:9090']  # local app on Linux host (make docker-up-infra + make run)
        labels:
          env: 'local'
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance
        regex: '(.*):\d+'
        replacement: '${1}'
